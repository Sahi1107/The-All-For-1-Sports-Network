// Teams tab — filled in Phase B (roster + per-match / per-tournament player stats).
export default function TournamentTeams({ tournamentId: _tournamentId }: { tournamentId: string }) {
  return (
    <div className="bg-card rounded-xl border border-line p-12 text-center text-sm text-gray-custom">
      Teams &amp; player stats — coming up in this build.
    </div>
  );
}

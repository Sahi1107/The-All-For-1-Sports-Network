// Fixtures tab — filled in Phase C (groups + knockout bracket from TrackerSession).
export default function TournamentFixtures(_props: { tournamentId: string; sport: string; isAdmin: boolean }) {
  return (
    <div className="bg-card rounded-xl border border-line p-12 text-center text-sm text-gray-custom">
      Fixtures &amp; bracket — coming up in this build.
    </div>
  );
}

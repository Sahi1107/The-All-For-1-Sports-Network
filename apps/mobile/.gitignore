# Expo / React Native
.expo/
dist/
web-build/

# Native build output (generated by `expo prebuild`; not checked in for a managed app)
/ios/
/android/

# Metro / Babel caches
.metro-health-check*
*.tsbuildinfo

# Env
.env*.local

# Firebase native config (per-project secrets — download from the console, never commit)
google-services.json
GoogleService-Info.plist

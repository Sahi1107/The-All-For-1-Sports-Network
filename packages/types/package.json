{
  "name": "@af1/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared canonical types (Prisma-derived enums) for web and mobile.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "gen": "node scripts/gen-enums.mjs",
    "build": "tsc",
    "test": "node --require ts-node/register --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}

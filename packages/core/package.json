{
  "name": "@af1/core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure shared domain logic (message policy, ranking config, positions) for server, web and mobile.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
